public class QuickMenuContextualDisplay
{
    public enum ContextualType
    {
        Undefined = -1,
        NoSelection,
        Tooltip,
        UserHover,
        UserSelected,
        Notification
    }
}